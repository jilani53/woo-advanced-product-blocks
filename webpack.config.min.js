const path=require("path"),fs=require("fs"),defaultConfig=require("@wordpress/scripts/config/webpack.config");delete defaultConfig.entry;const blocksDir=path.resolve(process.cwd(),"blocks"),entries={};fs.readdirSync(blocksDir).forEach(block=>{const entryFile=path.join(blocksDir,block,"index.js");fs.existsSync(entryFile)&&(entries[`${block}/index`]=entryFile)}),module.exports={...defaultConfig,entry:entries,output:{path:path.resolve(process.cwd(),"build"),filename:"[name].js"}};